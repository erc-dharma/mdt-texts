<?xml version="1.0" encoding="UTF-8"?>
<project basedir="." name="dharma-test">

    <!-- Macrodef taken from tlrobinson.net blog-->
    <macrodef name = "git">
        <attribute name = "command" />
        <attribute name = "dir" default = "" />
        <element name = "args" optional = "true" />
        <sequential>
            <echo message = "git @{command}" />
            <exec executable = "git" dir = "@{dir}">
                <arg value = "@{command}" />
                <args/>
            </exec>
        </sequential>
    </macrodef>
    <macrodef name = "git-clone-pull">
        <attribute name = "repository" />
        <attribute name = "dest" />
        <sequential>
            <git command = "clone">
                <args>
                    <arg value = "@{repository}" />
                    <arg value = "@{dest}" />
                </args>
            </git>
            <git command = "pull" dir = "@{dest}" />
        </sequential>
    </macrodef>

    <!-- git clone then pull -->
    <git-clone-pull repository="https://github.com/erc-dharma/project-documentation.git" dest="project-documentation" />

    <fileset id="csvFiles" dir="./csv" includes="*.csv"/>
    <fileset id="xmlFiles" dir="./temporary" includes="*.xml"/>

    <!-- Transformation csv vers XML -->
    <xslt force="true" style="./project-documentation/stylesheets/mdt_inscriptions/mdtText_start.xsl" in="fake-xml.xml" out="out-fake.xml" destdir="./temporary" extension=".xml" classpath="./project-documentation/editorialStylesheets/pipelineTools/saxon9he.jar">
        <factory name="net.sf.saxon.TransformerFactoryImpl"/>
    </xslt>

  <!-- Split  XML to several  -->
  <xslt force="true" style="./project-documentation/stylesheets/mdt_inscriptions/mdtText_splitting.xsl" destdir="./output" extension=".xml" useImplicitFileset="false" classpath="./project-documentation/editorialStylesheets/pipelineTools/saxon9he.jar">
      <fileset refid="xmlFiles"/>
      <factory name="net.sf.saxon.TransformerFactoryImpl"/>
  </xslt>
</project>
